include "zfc-axioms.rsn";

{a} = {b} ⟷ a = b;

a ⊂ b ∧ b ⊂ a → a = b;

let empty(e) ⟷ (∀(x) ~(x ∈ e));

empty(e) → e = ∅;

∀x. ~x = next(x);

minimal_natural(n) → n = N;

x = y → Pow(x) = Pow(y);

a = a ∪ a;

{a, x} = {a, y} ⟷ x = y;